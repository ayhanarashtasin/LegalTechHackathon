# Implementation status

Step 2 establishes the shared-record backend foundation. `NO` means the full requirement is not yet implemented, integrated, and testable through its intended journey. A foundation model or isolated API check alone does not qualify as `YES`.

Verified foundation: one Application-to-Case link; submission without a Case ID; authorised human acceptance; server-side role/office/assignment checks; append-only fact, consent, and safe-contact history; audit hash-chain checks; concurrent unique IDs. Citizen/provider journeys, contact disclosure, and document content handling are later steps.

Step 3 adds the non-AI workflow backbone: review and reasoned override, tasks with owner/next action, exact Application/Case ID search, document metadata versions, contact history, audit timeline, bounded shells for all eight provider roles, and one seeded fictional case. Browser proof (`npm run test:e2e`): a helpline-created application is reviewed and accepted by the DLAO through the same `applicationId`, and other roles cannot see it. Rows stay `NO` until their full persona/requirement journey lands in later steps.

Step 4 adds the deterministic (no-AI) `Call 16699 – Voice Access Prototype` page at `/voice`. It covers the simulation disclosure, a microphone-permission shell, separate live-voice/audio/transcript consent choices (placeholder wording), self vs representative, representation pending, identity incomplete, a safe-contact profile, read-back with correction, a minimal-data human callback (voice refused, person requested, or immediate danger), one Application ID with a DLAO review task, and an audited, integrity-checkable history. Contact logging now fails safe when an unknown person answers: neutral wording only, nothing disclosed, a safer follow-up task. Browser proof: Ripon finishes a representative intake with keyboard and accessible names only; all facts stay `REPRESENTATIVE_REPORTED` with applicant confirmation pending; the DLAO queue shows the one record; stored-audio refusal does not stop service; live-voice refusal reaches the callback route. A1/A2/B3 stay `NO` until live Bangla voice (Step 5) and Moyuri's own confirmation journey exist. **Changed 2026-09-23 by user decision:** the page is now a phone-call screen; the consent choices and the caller-requested callback button were removed, every call is recorded after the greeting's notice, and only immediate danger switches to the minimal-data callback. Browser proof now covers hang-up, the danger callback, and the stored recording instead of the consent refusals.

Step 5 adds optional Groq voice transcription and field extraction; IVR prompt playback uses the 19 recorded Bangla clips supplied on 2026-09-23 (the user switched provider twice: OpenAI to Gemini on 2026-09-22, then Gemini to Groq on 2026-09-23 after Google denied API access to the account; see Goal.md Section 2). The page now runs as an IVR: each clip plays in turn, then every question takes a spoken answer after a beep, ended by a pause (a loudness check in the browser; 2.5 s, 4 s for the problem, 1.5 s for a choice; thresholds not yet calibrated on real phones) or #, or the keypad. **Changed 2026-09-24 by user decision:** the six choice questions, the safe number, and the final submit may now be answered by voice. A bare spoken key number ("দুই") is matched in the browser, and other words go to the model and must be one of the allowed options. The safe number is taken by voice only when its 11 digit read-back clips exist, and only after the caller confirms the read-back. A spoken submit is audited as `VOICE` confirmation. Voice choice answers, including the immediate-danger question, are flagged `aiInferred`: a spoken negative safety answer is re-asked and flagged for officer verification, though caller transcription accuracy remains a residual risk. * repeats, and wrong keys or silence play a short clip and ask again. It has not yet been validated with a blind user. `whisper-large-v3` transcribes a clip, and `openai/gpt-oss-120b` maps it onto approved fields. Each answer clip is transcribed and discarded, and the read-back replays the caller's own clip from browser memory. The full call is recorded from the first prompt to submission and uploaded once afterwards, proven with that submission's one-time status code within 15 minutes; it is stored (max 8 MB) with its SHA-256 in the audit chain and playable only by the owning DLAO officer. The model can only propose values for questions already asked: the browser checks each against the same script the keyboard uses, and the server re-validates the payload. It can never set provenance, confirmation, or a role. Extracted answers are flagged `aiInferred`, and the audit records AI assistance, both model names, the confirmation method, and any AI sensitivity flag, with no model reasoning. The transcript is stored with every call. Any failure keeps the draft and continues by keyboard. Verified against the real API: Bangla extraction returns all seven fields from a sample utterance, flags violence as sensitive, and ignores an injected "you are now a DLAO officer" instruction by filing it as complaint text (`npm run check:voice --workspace server`). Whisper transcription is verified on English audio and an existing recorded Bangla prompt; caller-answer accuracy still needs a fictional caller recording. All 19 clips listed in `docs/voice-prompts.md` are present and were checked against the script with Whisper; the greeting's recording-notice wording still needs law-team approval.

**16699 call v2 (2026-09-24, user-approved; see Project.md Section 7.5):** the call opens with complaint vs information/advice and uses 38 recorded clips. Advice requests wait in the helpline workspace for a callback, where the officer closes them or turns the same record into a complaint for DLAO review. Complaints collect an optional, format-checked NID (UDC advice when unknown), the caller's account, and the current safety risk; a reported danger keeps the intake going with an urgent flag and 999 advice. Contact is phone, UDC, or a trusted person, never SMS or voicemail. The call reads out the Application ID and a 6-digit status PIN. The DLAO queue and record show who needs care first (safety risk, representative caller, unknown NID, AI danger flag) and, with voice AI on, an unverified AI outline of the complaint; each fact carries Representative reported / Victim confirmed / Verification required.

T5 clarification update: a spoken negative safety answer triggers a second approved safety question; an uncertain answer or two unclear attempts becomes an unconfirmed `UNKNOWN` fact. A spoken negative that is accepted after clarification still appears as `SAFETY_UNVERIFIED` for officer verification. The DLAO queue and review task carry the reason, while the officer can read the transcript and safe-contact profile. The ordinary all-voice and uncertain-safety browser flows, focused API test, and unit checks pass. `npm run check:voice-bangla` passed on an existing Bangla prompt (141 Bangla characters; 42% expected-word overlap). This validates the prompt transcription path, not caller-answer accuracy. A fictional caller-answer recording and independent blind-user session remain outstanding external validation.

Step 6 adds an office-scoped daily queue with reasons, search, ageing, and structured-data report; officer priority override with an audit event; redacted case-support reconstruction; and a helpline status lookup on the same Application/Case. The lookup requires an exact ID, one-time displayed code, explicit human-verification attestation, and an active permitted safe-contact route. It returns generic status/next step, and Step 9 adds hearing date and next action when recorded. All results are logged in the existing contact/audit histories. Referral-waiting and lawyer-update-overdue categories are live when their underlying events exist. Caller-verification policy/wording still needs law-team approval, and code possession alone is not identity proof. Older records have no lookup code. B1 and B3 remain `NO` because those full persona dependencies are not complete; B7's structured record/search/version/task/report journey is now testable.

Step 7 adds a bounded UDC assisted-intake route: free-service notice, separate applicant/helper/translator/typist identities, Marma original versus Bangla translation, consent and confirmation states, a case-type checklist, and an in-person safe-contact profile that does not promote the helper's phone into applicant contact. Encrypted IndexedDB drafts and mutation IDs survive a mid-intake network drop, sync three records once each, detect a stale version, show both versions, and require a human reasoned choice. The audit hash chain verifies. The production PWA installs, caches only the static shell, loads that shell offline, and purges local drafts on logout. Light mode skips recorded Bangla prompts and audio-file probes; provider pages load on demand for both modes. The current same-profile throttled check is documented in [docs/pwa-performance.md](docs/pwa-performance.md). Six fictional text files yield a provisional cited briefing with a missing checklist item and an unreadable deed; an owning officer verifies and approves it. The revised Groq quote-selection contract passed a live fictional-text check (`npm run check:document-ai`); deterministic cited extracts remain the fallback. This is a text-only fictional prototype, not scanned-PDF extraction or a real shared-device deployment. The UDC correction window is a demo 24 hours pending an approved policy.

T6 source-grounding update: each new briefing point is an exact quotation tied to a readable standard document version and line. Invalid model quotations or source IDs fall back to deterministic cited extracts. The checklist distinguishes missing, uncertain, and tagged-readable items, while reminding officers that tags require verification. Long sources beyond 12 non-empty lines or 240 characters per line are flagged. Owning DLAO officers can open the full stored source text and approve with an audited reason; same-office case support sees a read-only briefing. The source guard unit check, focused API check, browser scenario, and live fictional Groq contract check pass. Existing approved briefings retain their legacy display until regenerated.

Step 8 adds the fictional Nabila case (seeded) on the same Application/Case. Demo urgency rules show concise reasons with evidence references, and the officer records the final priority. Restricted evidence is classified at first upload and opens only through an explicit per-user grant or an open referral naming the responsible receiving DLAO. Office role alone is never enough, and every open or denial goes to an append-only access log. A referral package carries the reason, relevant history, chosen standard documents, sending and receiving offices, responsible actor, expected action, and acknowledgement deadline. Restricted evidence goes only with a recorded necessity reason, and safe-contact rules travel with it (not the number). The receiving office acknowledges, accepts, or returns with a reason; both offices see the status. When the deadline passes unacknowledged, a server timer adds one follow-up task and a `SYSTEM` audit event. After two returns, the system escalates, blocks further transfers, and waits for a reasoned human routing decision, which then binds the next referral. The referral-waiting queue is now live. API and browser tests prove each acceptance check. Placeholders pending law-team approval: urgency criteria, uploader-only grants, the acknowledgement deadline, the threshold of two, and who holds routing authority. The overdue timer runs inside a single API instance. There is no reclassification, colleague grant, or post-acknowledgement response deadline yet.

Step 9 adds the fictional seven-month Malek case, a caller-initiated status route safe for phone or in-person use, a hearing date and applicant-safe next step, and a failed-contact record that discloses nothing and creates a safer follow-up. DLAO officers manage the same Case's lawyer offer, required update schedule, change-request review, and stage-based payment status; panel lawyers use an office-scoped worklist and may accept/decline and submit progress reports. Two consecutive missed required updates create review tasks and a global new-assignment hold for that lawyer; current assignments stay active. A DLAO may continue or lift the hold, while reassignment is a separate human action. Citizen portal requests now require ownership of an accepted Case with an accepted lawyer; another citizen receives a 404, pending offers do not count as representation, and an approved request cannot be duplicated while replacement is pending. The portal shows the actual applicant, Case, priority, lawyer, and review state. Officer payment view shows the latest status for each stage and the complete append-only event history, including prior assignments. The digital citizen submission starts a valid audit sequence. Payment events are reconciliation records only and work can be reconciled after reassignment. The seeded fictional scenario's one-time lookup code remains in the ignored local demo-credentials file. API and browser acceptance tests passed, including a keyboard-only lookup, citizen request and officer review, payment-stage history, a two-miss hold, and an on-time update that breaks the miss sequence. Hold authority, the threshold, and due-process requirements remain pending legal/administrative verification; the timer runs in one API instance. No outbound contact, real payment, misconduct finding, panel removal, or automatic financial recovery occurs.

Step 10 adds three separate accepted fictional factory-fire Cases in one same-office related-incident group. One readable standard document is stored on one Case and shared by reference; group view keeps member records, instructions, outcomes, and restricted evidence separate. Owning-office case-support staff can read group members and already-shared evidence, while only DLAO officers can create groups or share evidence. Twelve synthetic duplicate-review profiles include an exact/typo candidate and two similar-but-different traps. Same-office DLAO suggestions use deterministic normalized name similarity plus exact contact, date-of-birth, and district attributes; the score is an uncalibrated ranking, not a probability. Case-support staff can see same-office candidate names, IDs, ranking, and review status, but not field-level comparison details or review reasons; only DLAO officers can review pairs. Human decisions require a reason, are audited on both Application records, and never merge or reject anything. API and browser checks cover the shared reference, privacy boundaries, duplicate review, and traps. The prototype's heuristic weights and suggestion threshold are unvalidated and must not be treated as identity evidence.

Step 11 adds three parallel, structured triage components (case categorization, process/safety checks, urgency/routing) with a deterministic fallback. Groq requests contain only allowlisted category, provenance, safety/workflow flags, and document counts; applicant names, contact details, DOB, complaint text, and document contents are excluded. Strict output validation rejects invented evidence and unsafe decisions. The orchestrator surfaces HIGH-vs-LOW disagreement and a separate routing/jurisdiction review prompt from recorded referral returns, open escalation tasks, and human routing decisions. A DLAO records a reasoned category/disposition, with stale-assessment protection and audit history. This review note does not change Application priority, routing, eligibility, or outcome. Five fictional cases and a conflict are covered by API/browser tests; one configured Groq run was accepted. No new credential was needed.

Step 12 adds a same-Case mediation workflow with mediator claim, notice/scheduling outcomes, attendance, three typed settlement templates, schema-constrained AI assistance or deterministic fallback, visible inconsistency warnings, human mediator review, and separate understanding/consent acknowledgements. The mediator issues separate short-lived, single-use party codes; each party views and confirms the approved draft in a separate browser action. The signature journey uses browser P-256 signatures, server-side verification, version/hash binding, encrypted offline draft/signature storage, idempotent sync, and an independent changed-copy verifier. A mediator session can submit only the mediator signature, after both party signatures. API/browser acceptance scenarios cover unauthorized office access, AI allowlisting and excluded identifiers (provider mocked in test), consent denial, invalid signatures, asynchronous separate-party signing, offline retry, mediator ordering, tamper failure, legal-review state, and separately gated CLAO certification. Codes and signatures alone never establish identity or legal effect; commencement/date/area applicability is not independently verified, and certification is not represented as a court decree. See `docs/legal-assumptions.md`.

Step 13 hardens shared paths: API/health responses receive basic security headers and `no-store`; demo login is throttled after five failed attempts per username/source IP; prohibited-channel contact success records are rejected while unsafe-answer failures remain loggable; text uploads are bounded at 50 KB UTF-8 behind a 128 KB JSON cap; SPA route changes focus the main landmark. `docs/threat-model.md` now records assets, attacker capability, prevention, detection/recovery, and residual risk for the required scenarios. Verification: `npm run lint`, production client build, `npm audit --workspaces` (0 known vulnerabilities), Step 2–8 and Step 10–12 API/unit tests, Step 9 API test after fixing the unsafe-answer logging edge case, and Step 13 browser check (headers, throttle, alert semantics, route focus, Bangla language tag, 320px reflow). Manual screen-reader and external penetration tests were not performed; hosting must add matching static-site headers and a distributed limiter before scale.

Step 14 makes the demo seed repeatable and adds fictional assisted-intake, voice-intake, and jurisdiction-escalation records. The Atlas seed command was run twice successfully; the existing data was preserved. `docs/demo-flows.md` now contains six click-by-click flows and all 12 bad-day checks. `docs/architecture.md` and `server/.env.example` now describe the Groq-based optional integrations and current deployment limits.

**Spoken status for A5 (added 2026-09-25 by user decision; see Goal.md Section 2).** The home page's Track card has **Ask by Voice**: the caller says what they want, their Application/Case number (read back and confirmed), and their 6-digit PIN (never read back or shown), then hears the stage, the hearing date, and a Bangla next step, spoken by a local BanglaTTS service (`tts-service/`, `npm run tts`) and shown as text. The sentence comes from fixed templates over the verified record and names no person, matter, lawyer, or office. Whisper turns are primed with server-held yes/no or digit hints; digits, yes/no, and status words are matched in the browser, English digit words in Bangla script included; only an opening with no status word goes to the text model, which answers one boolean and never handles numbers. The number and PIN can be typed during the call, and each turn but the PIN shows what was heard. Because the phone or screen may be shared (Malek's number is a shop's), the call asks whether the caller can listen privately before the PIN and says nothing about the case without a clear yes, and the public tracker (typed and spoken) no longer returns the applicant's name, the legal matter, or the lawyer's name.

**Contact log (added 2026-09-26).** Each contact attempt now records who answered (the applicant, someone else with an optional note such as "shop owner", or nobody). When someone else answered, the officer must state whether anything about the case was disclosed; it was previously stored as "no" automatically. A stated disclosure creates a review task. When the applicant was reached, the officer states whether the status was explained. No answer, or someone else answering, creates a follow-up task dated at the next attempt the officer plans, shown beside the safe time. The log shows each attempt with its number (for officers), so "how often did we try?" is answered on the record. The simulated unknown-person call now opens the form with the neutral words instead of logging for the officer. The seeded Malek case shows the failed shop-line call and a later call that reached him with the status explained. Verified by `server/src/contactLog.test.js` and the Step 2, 4, and 9 tests. The seeded Malek case now has a 6-digit PIN, a Sunday–Thursday hearing, and a Bangla next step. A per-ID lockout (ten wrong codes an hour, typed and spoken routes alike) and a shared per-address limit now guard the public lookup, as a code comment required before any caller-facing PIN lookup.

**Lawyer update reminders and activity summary (added 2026-09-26).** When a panel-lawyer update is overdue, the DLAO officer sees how late it is on the record and in the overdue alert (`Lawyer update overdue`), and can request it with one click instead of phoning the lawyer (`POST /api/lawyers/applications/:applicationId/updates/:updateId/reminders`). This places a reminder note directly on the lawyer's worklist and assigned case page (showing the reminder count and timestamp, with no officer identity), limited to once per 24 hours. The officer can also review the lawyer's cross-case activity summary (`GET /api/lawyers/panel-lawyers/:id/activity`), showing on-time, late, and overdue updates across active cases, with an explicit notice that this is not a finding of misconduct. Verified: client 27/27 and server 50/50 (`npm test`, all Step 2–12 database tests included); `server/src/lawyerFollowUp.test.js`; the full browser suite, 20 passed and 1 opt-in skipped (`tests/e2e/step9.spec.js` included); lint and build clean.

Coverage below records feature state. The last full `npm run test:e2e` run (2026-09-26) completed with **20 passed, 1 skipped**. The skipped test is opt-in live voice.

## A1-A5, B1-B7, T1-T11 coverage

| Requirement | Implemented | Integrated | Testable | Route, page, or API | Demo seed | Automated test | Manual jury step | Known limitation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A1 Moyuri | NO | NO | NO | `/voice`; `/api/voice/intakes`; officer fact/contact APIs | `STEP4_MOYURI_RIPON` | `tests/e2e/step4.spec.js`; `server/src/step2.test.js` (representative path only) | Flow 1, steps 1-7; keep applicant confirmation pending | No applicant self-service confirm/correct/withdraw journey; withdrawal is absent. |
| A2 Ripon | NO | NO | NO | `/voice`; keypad IVR with all 19 recorded prompts | `STEP4_MOYURI_RIPON` | `tests/e2e/step4.spec.js` covers keyboard only; it does not prove the blind-user requirement | Do not claim pass; rehearse a meaningful Bangla task with a blind user/screen reader and no sighted helper | Screen-reader/low-vision testing with a blind user is missing. On NVDA, number keys in browse mode jump between headings, so a screen-reader caller should use the on-screen keypad buttons or focus mode. |
| A3 Nabila | YES | YES | YES | `/applications/:applicationId`; referral and restricted-evidence APIs | `STEP8_NABILA` | `tests/e2e/step8.spec.js`; `server/src/step2.test.js` | Flow 5, steps 1-5 | Urgency criteria, grant scope, return threshold, and routing authority remain policy assumptions. |
| A4 Nuching | YES | YES | YES | `/assisted`; assisted intake, revision, conflict, and document APIs | `STEP7_NUCHING` | `tests/e2e/step7.spec.js`; `server/src/step2.test.js` | Flow 2, steps 1-8 | Fictional `.txt` only; no scanned-PDF/OCR support; shared-device deployment is not assessed. |
| A5 Malek | YES | YES | YES | `/cases/:caseId`; lawyer case-plan/status APIs; home Track card **Ask by Voice** with `/api/voice/{transcripts,requests,prompts,status}` | `STEP9_MALEK` (6-digit PIN) | `tests/e2e/step9.spec.js`; `server/src/step9.test.js`; `tests/e2e/voice-status.spec.js`; `server/src/voiceStatus.test.js`; `server/src/voiceStatusLookup.test.js`; `client/src/utils/voiceStatusCall.test.js` | Flow 6, steps 1-5 | Hold authority and due-process policy need validation; no real contact, payment, or lawyer sanction. Spoken status is not yet validated with a real caller's voice; its BanglaTTS voice is demo-only (non-commercial model licence). |
| B1 DLAO officer | YES | YES | YES | `/`; `/applications/:applicationId`; queue, review, priority, facts, tasks, referrals, triage APIs | Nabila, Malek, factory, triage, and mediation records | `tests/e2e/step3.spec.js`, `step6.spec.js`, `step8.spec.js`, `step10.spec.js`, `step11.spec.js`; API tests | Flows 3-5 | Urgency and routing policy still need approval. |
| B2 Mediator | YES | YES | YES | `/applications/:applicationId`; mediation API | `STEP12_MAINTENANCE`, `STEP12_PROPERTY`, `STEP12_LABOUR` | `tests/e2e/step12.spec.js`; `server/src/step12.test.js` | Flow 4, steps 1-8 | Signature is not proof of identity, capacity, consent, or enforceability; CLAO applicability is attested, not independently verified. |
| B3 Helpline | YES | YES | YES | `/`; `/voice`; status lookup and application APIs | Fictional provider actors; E2E creates fictional lookups | `tests/e2e/step3.spec.js`, `step4.spec.js`, `step6.spec.js`; `server/src/step2.test.js` | Flows 1 and 6 | Caller-verification wording needs legal approval; code possession is not identity proof; no outbound contact. |
| B4 UDC operator | YES | YES | YES | `/assisted`; server-side UDC read restrictions | `STEP7_NUCHING` | `tests/e2e/step7.spec.js`; `server/src/step2.test.js` | Flow 2, steps 1-6 | Only bounded fictional prototype workflows; no live UDC system. |
| B5 Panel lawyer | YES | YES | YES | `/cases/:caseId`; `/api/lawyers/*` worklist/update APIs | `STEP9_MALEK` | `tests/e2e/step9.spec.js`; `server/src/step9.test.js`; `server/src/lawyerFollowUp.test.js` | Flow 6, steps 2-4 | Inactivity thresholds/hold authority are demo policy; no automatic misconduct or reassignment. |
| B6 Receiving DLAO | YES | YES | YES | `/referrals/:referralId`; receiving-office referral APIs | Nabila and `STEP14_RAHIM_PING_PONG` | `tests/e2e/step8.spec.js`; `server/src/step2.test.js` | Flow 5, steps 2-5 | Referral is an internal prototype workflow, not a jurisdiction determination. |
| B7 Case support | YES | YES | YES | `/`; search/history/reconstruction APIs | Dynamic fictional E2E Application | `tests/e2e/step6.spec.js`; `server/src/step2.test.js` | Flow 3, step 5 | Report is structured demo data, not an official statistical return. |
| T1 Lawyer accountability | YES | YES | YES | Citizen `/api/citizen/cases/:applicationId/lawyer-change`; DLAO lawyer-management, payment-history, and hold-review APIs | `STEP9_MALEK`; fictional citizen portal Case | `tests/e2e/step9.spec.js`; `server/src/step9.test.js`; `server/src/lawyerFollowUp.test.js` | Flow 6, steps 2-7 | Two-miss threshold and review authority are unapproved demo assumptions. |
| T2 Jurisdiction ping-pong | YES | YES | YES | Referral panel/API plus human routing decision | Nabila; `STEP14_RAHIM_PING_PONG` | `tests/e2e/step8.spec.js`; `server/src/step2.test.js` | Flow 5, steps 2-5 | Two-return threshold and responsible authority need policy/legal confirmation. Transfers require a decision covering the latest return; only an open escalation permits a new decision, and inactive receiving users are excluded. |
| T3 Related incidents | YES | YES | YES | `RelatedIncidentPanel`; `/incidents/:groupId` and evidence-link APIs | Three `STEP10_FACTORY_FIRE_*` Cases | `tests/e2e/step10.spec.js`; `server/src/step10.test.js` | Flow 3, step 3 | DLAO officers create groups/share evidence; same-office case-support staff have read-only access. Only human-linked standard evidence is shared; no automatic grouping. |
| T4 Duplicate review | YES | YES | YES | `DuplicateReview`; role-scoped candidate and review APIs | Twelve `STEP10_DUP_*` profiles | `tests/e2e/step10.spec.js`; `server/src/step10.test.js` | Flow 3, step 4 | Case support sees a redacted ranking only; DLAO officers see comparison details and decide. Ranking weights/threshold are unvalidated and never establish identity. |
| T5 Bangla intake agent | YES | YES | YES | `/voice`; `/api/voice/answers`, `/api/voice/intakes`; DLAO safety handoff | `STEP4_MOYURI_RIPON` | `server/src/scripts/checkVoiceAi.js`; `server/src/scripts/checkBanglaAudio.js`; `tests/e2e/step5.spec.js`; `server/src/step2.test.js` | Flow 1, steps 1-8; show an ordinary intake and an uncertain-safety handoff | Controlled IVR, with repeated/unclear safety answers flagged for human verification. Bangla STT passed on a recorded prompt; caller-answer accuracy and independent blind-user use still need validation. The recorded safety prompt does not announce keypad option 3, though the screen reader and on-screen keypad do. |
| T6 Document agent | YES | YES | YES | `/applications/:applicationId`; briefing/document/source-text APIs; read-only case-support view | Nuching and six fictional text samples | `tests/e2e/step7.spec.js`; `server/src/step2.test.js` | Flow 2, steps 7-8 | Text-only; unreadable/scanned content is not inferred. Every new point quotes a specific source version and line; long sources are flagged for full officer review. |
| T7 Settlement draft | YES | YES | YES | Mediation page; draft/review APIs; versioned fictional reference catalog | Three `STEP12_*` mediation examples | `tests/e2e/step12.spec.js`; `server/src/step12.test.js`; `server/src/services/settlementTemplates.test.js` | Flow 4, steps 4-8; `docs/settlement-templates.md` | All three technical draft paths are covered. AI only suggests; template wording and formalities still require law-team approval before real use. |
| T8 Multi-agent triage | YES | YES | YES | `TriagePanel`; triage/decision APIs; separate evidence-backed routing review | Five cases, including `STEP11_TRIAGE_CONFLICT` and returned-referral fixtures | `tests/e2e/step11.spec.js`; `server/src/step11.test.js` | Flow 3, step 2 | Component rules/weights and jurisdiction prompts are demo decision support; authorised staff and approved policy determine the route. |
| T9 Offline sync | YES | YES | YES | `/assisted`; encrypted IndexedDB, sync/conflict APIs | `STEP7_NUCHING` | `tests/e2e/step7.spec.js`; `server/src/step2.test.js` | Flow 2, steps 4-6 | Browser encryption cannot secure a compromised/shared device or protect a shared passphrase. |
| T10 Low-bandwidth PWA | YES | YES | YES | Installable static-shell service worker; adaptive/user-selected light mode; lazy provider pages | `STEP7_NUCHING` | `tests/e2e/step7.spec.js`; `tests/pwa/offline.spec.js` | Flow 2, step 8; [same-profile measurements and cache policy](docs/pwa-performance.md) | Both modes use the same initial shell; light mode saves recorded-prompt requests. No field-device calibration yet. |
| T11 E-signature | YES | YES | YES | Mediation page, public `/mediation/sign`, verifier; code issue, sign, sync, verify, and CLAO APIs | Three `STEP12_*` mediation examples | `tests/e2e/step12.spec.js`; `server/src/step12.test.js` | Flow 4, steps 5-8 | A code holder can sign, but code possession and cryptography do not verify identity or legal validity. |

## Golden-thread checks

| Requirement | Implemented | Integrated | Testable | Evidence | Remaining limitation |
| --- | --- | --- | --- | --- | --- |
| G1 One record | YES | YES | YES | Step 3/4/7 shared Application journeys | Applicant confirmation and withdrawal remain incomplete. |
| G2 Provenance | YES | YES | YES | Step 4/7 provenance UI and API audit tests | No applicant self-service correction journey. |
| G3 Safe contact | YES | YES | YES | Step 4 unknown-answer and Step 9 failed-contact tests | Law-team approval of caller-verification policy is pending. |
| G4 Accessibility | NO | YES | YES | Step 4 keyboard-only path; Step 13 focus/reflow checks | Manual screen-reader and low-vision testing is outstanding. |
| G5 Human control | YES | YES | YES | Step 6, 8, 11, and 12 decision gates | Some authority thresholds are still demo assumptions. |
| G6 Document control | YES | YES | YES | Step 7 citations, unreadable-file path, and version history | Text files only; no scanned-PDF extraction. |
| G7 Tracked responsibility | YES | YES | YES | Queue tasks, referral deadlines, and lawyer update hold tests | Timers run in one API instance. |
| G8 Resilience | YES | YES | YES | Step 7 retry/conflict PWA tests and Step 12 offline-signature test | An earlier run saw transient Atlas timeouts; the focused and full reruns passed. |
| G9 Role-based privacy | YES | YES | YES | Step 8 denied restricted read and audit tests | No external penetration test performed. |
| G10 Audit | YES | YES | YES | Step 2-13 audit reconstruction/integrity checks | Hash chain lacks an external anchor and is not admin-proof. |

## Step 15/16 checkpoint — local QA passed; public deployment and manual validation pending

- Production packaging now serves `client/dist` from Express only under `NODE_ENV=production`; extensionless client routes fall back to the shell, `/api` and `/health` do not, and the client CSP supports the service worker and blob audio without loosening the API CSP. `npm run check:production-static` passed before the separate-hosting changes (build plus shell, asset, route, CSP, and API/health isolation checks). Cross-origin API responses now allow only the configured `CLIENT_ORIGIN`.
- Passed locally this checkpoint: `npm run lint`; `npm run check:production-static` (build plus production Express shell test); `npm audit --workspaces` (0 known vulnerabilities); client unit tests (4/4); full browser suite before cleanup (16 passed, 1 opt-in live-voice test skipped); production PWA test (1/1). At 150 ms/50 KB/s, normal/light shell loads were 2.942/2.935 seconds and mode toggles 86/71 ms. The build emits a React Router `use client` directive warning but succeeds.
- Standard `npm test` passed: client 4/4 and server 22/22. Server integration tests run serially against guarded random test databases to avoid intermittent Atlas contention.
- Production-mode runtime smoke connected to Atlas, returned `/health` 200, and served `/`, `/voice`, the manifest, and service worker with the client CSP. It bound only to `127.0.0.1` for the check and was stopped; the port was verified closed. Earlier development/production binding smokes also passed, and production demo login returned `503 DEMO_AUTH_DISABLED`.
- Secret hygiene check passed: `server/.env` exists, is ignored and untracked; no MongoDB/Groq environment marker appeared in the built client bundle.
- Not run: public URL smoke test (there is no deployment), manual screen-reader/low-vision check, external penetration test, and a live Groq browser test (opt-in).
- A2/T5 voice accessibility remains incomplete: the 19 prompt clips now exist in `client/public/audio/`, but the keyboard-only browser test is not evidence of a blind user's independent Bangla task.
- The referenced OWASP review playbook/template is absent from the project skill directory. A bounded source-based OWASP review was recorded in `docs/threat-model.md`; this is not a penetration test or formal security assessment.
- Production host binding is prepared: development stays on loopback; production defaults to all interfaces and accepts optional `HOST`. Vercel CLI is installed, but its identity probe stalled and no authenticated host session could be verified; Render, Railway, and Fly CLIs are absent. No deployment target or public URL exists. Step 15 still needs connected hosting accounts and a human-approved access model; use fictional data only.
- Vercel/Render preparation now includes an API origin setting, exact-origin CORS, a Vercel SPA rewrite, opt-in production staff login for fictional seeded users (quick-fill endpoint remains off), and visible/retryable call-recording upload failures. An identical recording retry does not create a second audit event. `npm run lint`, `npm run build`, and `git diff --check` passed for these changes; public host, full API/browser checks, production static headers on Vercel, and real microphone/upload behavior remain unverified.
